Warm content container. Flat cream + hairline by default; `elevation="raised"` for floating white cards; tones `sage` / `clay` / `inverse` for section variety.

```jsx
<Card tone="sage" padding={32}>
  <h3>Gemeinsam wachsen</h3>
</Card>
```
